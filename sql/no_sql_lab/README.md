# NoSQL Lab

Repositorio con los materiales del laboratorio sobre NoSQL (MongoDB y Redis).

- Archivos MongoDB: `*.mongodb`, `example.mongodb.js`
- Archivos Redis: `*.redis`
- Documentación: `INTRODUCTION TO NoSQL DATABASES (MONGODB AND REDIS).md`, `lab_101.md`

Instrucciones rápidas:

1. Abrir la carpeta en tu editor: `c:\Users\samla\Desktop\sql\no_sql_lab`
2. Para inicializar git (si aún no está inicializado):
```
cd c:\Users\samla\Desktop\sql\no_sql_lab
git init
git add .
git commit -m "Initial commit: NoSQL lab files"
```
3. Para añadir un remoto en GitHub y hacer push:
```
git remote add origin https://github.com/<usuario>/<repo>.git
git branch -M main
git push -u origin main
```

Si quieres, puedo crear el repo en GitHub por ti (necesitaré permisos o un token), o sólo dejar todo listo localmente.

---
Generado automáticamente: añade más instrucciones si las necesitas.
