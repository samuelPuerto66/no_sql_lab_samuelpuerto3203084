```bash
# volume
docker run -it --rm -p 27017:27017 -v "$(pwd)/mongo_data:/data/db" -e MONGO_INITDB_ROOT_USERNAME="admin" -e MONGO_INITDB_ROOT_PASSWORD="miSecretoPassword" --name some-mongo mongo:latest

docker run -it --rm -p 27017:27017 -e MONGO_INITDB_ROOT_USERNAME="admin" -e MONGO_INITDB_ROOT_PASSWORD="miPassword" --name sample-mongo mongo:latest
```

```bash
docker run -it --rm -p 6379:6379 -v "$(pwd)/redis_data:/data" -e REDIS_PASSWORD="miPassword" --name some-redis redis/redis-stack:latest

```

```
mongodb://admin:miPassword@localhost:27017/
```

```
mongodb://admin:miPassword@localhost:27017/turnos_db?authSource=admin
```

```bash
docker run -it --rm -p 6379:6379 --name sample-redis redis:latest redis-server --requirepass "miPassword"
```
